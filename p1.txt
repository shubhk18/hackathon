#!/bin/bash

# Input date in yymmdd format
input_date="250103"

# Convert to yyyy-mm-dd format
formatted_date=$(date -d "20${input_date:0:2}-${input_date:2:2}-${input_date:4:2}" +"%Y-%m-%d")

echo "$formatted_date"