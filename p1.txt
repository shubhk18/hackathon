#!/bin/bash

# Define a Bash variable
my_variable="some_value"

# Pass the Bash variable as a bind variable
sqlplus -s username/password@database <<EOF
VARIABLE var1 VARCHAR2(100)
BEGIN
  :var1 := '$my_variable';
END;
/

SELECT * FROM your_table WHERE your_column = :var1;

EXIT;
EOF