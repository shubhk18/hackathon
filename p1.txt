SET PAGESIZE 0          -- Disable pagination and column headers
SET LINESIZE 1000       -- Prevent line wrapping; adjust as needed
SET FEEDBACK OFF        -- Suppress feedback like "N rows selected"
SET TRIMSPOOL ON        -- Remove trailing spaces in the output

SPOOL employee_names.txt -- Start spooling to the output file

SELECT name
FROM employees;         -- Replace with your actual query and table name

SPOOL OFF               -- Stop spooling and save the file